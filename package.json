{
  "name": "local-pro-sites-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Complete SaaS platform for web design agencies",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/api\" \"npm run dev -w packages/web\"",
    "build": "npm run build -w packages/api && npm run build -w packages/web",
    "start": "npm run start -w packages/api",
    "db:generate": "npm run db:generate -w packages/api",
    "db:migrate": "npm run db:migrate -w packages/api",
    "db:studio": "npm run db:studio -w packages/api"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
